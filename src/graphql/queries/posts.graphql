query AllPosts($count: Int!) {
    allPosts(count: $count) {
    title
    body
    published
    createdAt
    likelyTopics {
      label
      likelihood
    }
    author {
      id
      firstName
      lastName
      email
      avatar
    }
  }
}