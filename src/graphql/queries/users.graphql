query AllUsers($count: Int!) {
  allUsers(count: $count) {
    firstName
    lastName
    email
    avatar
  }
}

query User($id: ID!) {
  User(id: $id) {
    firstName
    lastName
    email
    avatar
  }
}